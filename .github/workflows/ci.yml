name: PyGoat

on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: lint terraform files
        uses: terraform-linters/setup-tflint@v4
      - run: tflint --chdir=EKS-Terraform

      - name: terraform fmt
        run: terraform fmt --check --chdir=EKS-Terraform

      - name: terraform validate
        run: |
          terraform init --chdir=EKS-Terraform
          terraform validate --chdir=EKS-Terraform

      - name: install kube-linter
        run: |
          curl -s https://raw.githubusercontent.com/stackrox/kube-linter/main/install.sh | bash
          sudo mv kube-linter /usr/local/bin/
      
      - name: lint kubernetes manifests
        run: kube-linter lint ./manifests


  test:
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v4

      - name: installer docker et docker compose
        run: | 
          chmod +x scripts/docker_install.sh
          ./scripts/docker_install.sh

      - name: Test my app
        run: | 
          docker build -t pygoat-test .
          docker run pygoat-test pytest
          docker ps -a 
          docker rm $(docker ps -a -q)


  
  build:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4

      - name: installer docker et docker compose
        run: | 
          chmod +x scripts/docker_install.sh
          ./scripts/docker_install.sh

      - name: Build my app
        run: | 
          docker build -t pygoat-prod .
      
      - name: login to docker hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: push to docker hub
        run: | 
          docker push pygoat-prod:latest 


  

